# OS junk
.DS_Store

# Editor/auto-save backup copies (timestamped duplicates)
* [0-9][0-9]-[0-9][0-9]-[0-9][0-9]-*

# Playwright — match at any depth so every suites/<suite>/ is covered
node_modules/
test-results/
reports/
playwright-report/
blob-report/
playwright/.cache/
playwright/.auth/

# Local env / auth
.env
auth/
