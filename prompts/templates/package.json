{
  "name": "ADAPT-<site>-playwright",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "e2e": "playwright test",
    "e2e:basic": "playwright test e2e/specs/basic",
    "e2e:orders": "playwright test e2e/specs/orders",
    "baseline": "playwright test --grep @visual --update-snapshots",
    "report": "playwright show-report e2e/reports",
    "setup:browsers": "playwright install chromium",
    "typecheck": "tsc --noEmit",
    "lint": "woolverine-lint e2e/specs && npm run typecheck"
  },
  "dependencies": {
    "@saucal/woolverine": "^ADAPT-LATEST-MINOR",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
