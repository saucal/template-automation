# tests/.gitignore — node_modules and .env are root-level: add both to the site repo's own .gitignore.
auth/
reports/
test-results/
# Visual baselines carry a platform suffix and are re-seeded per machine (or cached by CI)
**/*-snapshots/
